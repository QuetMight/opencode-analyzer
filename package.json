{
  "name": "opencode-analyzer",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev": "bun run --cwd packages/web dev & bun run --cwd packages/server dev",
    "build": "bun run --cwd packages/shared build && bun run --cwd packages/opencode-observer build && bun run --cwd packages/server build && bun run --cwd packages/web build",
    "lint": "eslint packages --ext .ts,.tsx",
    "format": "prettier --write ."
  },
  "devDependencies": {
    "eslint": "^8.56.0",
    "prettier": "^3.2.0",
    "typescript": "^5.3.0"
  }
}